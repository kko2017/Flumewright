# Build
bin/
obj/
[Bb]in/
[Oo]bj/

# Test results
TestResults/
*.trx
coverage*.xml

# IDE / OS
.vs/
.idea/
*.user
.DS_Store

# Certificates & keys (never commit — key leakage risk; see plan 7.1)
*.pfx
*.key
*.pem
*.crt
*.cer
*.p12
certs/

# Logs (never commit; see plan §4 logging)
logs/
*.log

# Local config
appsettings.Development.json
*.local.json

# Personal working drafts (numbered source docs; repo uses docs/ only — see plan language policy)
/[0-9][0-9]-*.md
/FW_Context.md
/ai-collaboration.en.md

# Ad-hoc diff dumps (work scratch)
*_diff.txt

# Tooling / environment scratch (not part of the project)
dotnet/
.github/copilot-instructions.md
.devcontainer/devcontainer-lock.json
